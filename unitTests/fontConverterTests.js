const assert = require('chai').assert;
const FontConverter = require('../lib/fontConverter');

describe('FontConverter', function () {
    it("constructor - check default font is used when not font is provided", function () {
        //Act
        const fontConverter = new FontConverter();
    });
});